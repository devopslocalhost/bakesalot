name: SLACK

# Controls when the workflow will run
on:
  # Triggers the workflow on pull request events but only for the "main" branch
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "slack"
  slack:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Notify slack
        env:
          SLACK_BOT_TOKEN: ${'xoxb-481636866753-4086902024289-cXIJXVXHoddX7jTO4yWesp9u'}
        uses: pullreminders/slack-action@master
        with:
          args: '{\"channel\":\"C041QMF3TE3\",\"text\":\"Mubarak Abubakar Bunza, There is New Pull Request Has Been Made\"}'

